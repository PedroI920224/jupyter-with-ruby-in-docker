# jupyter-with-ruby-in-docker
this is a docker-compose with Ruby and docker-compose to write down documentation in Jupyter
commands:

```
docker-compose up
```

visit http://localhost:8888

Add the token
